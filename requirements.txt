# ===== Обязательное =====
aiogram>=3.7
aiosqlite>=0.20

# ===== Опционально: авто-цензура фото 18+ =====
# Без неё фото идут на ручную модерацию тебе в ЛС (кнопки ✅/❌).
# Вариант 1 (рекомендуется, точнее) — NudeNet:
nudenet>=3.4
# Вариант 2 (запасной, легче) — раскомментируй, если не ставишь NudeNet:
# opennsfw2>=0.10

# ===== Опционально: проверка текста НА фото (OCR) =====
# Нужен системный пакет tesseract:
#   sudo apt install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng
pytesseract>=0.3.10
pillow>=10.0
